<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Reborn Pilates | Tenafly · Cresskill</title>
<meta name="description" content="Classical Pilates studio in Tenafly/Cresskill, NJ. Private & duet sessions. Book a trial class today." />
<meta property="og:title" content="Reborn Pilates" />
<meta property="og:description" content="Classical Pilates studio in Tenafly/Cresskill, NJ. Private & duet sessions." />
<meta property="og:type" content="website" />
<meta property="og:url" content="https://www.reborn-pilates.com" />
<meta name="theme-color" content="#0ea5a3" />
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
--brand:#0ea5a3; /* light teal */
--brand-dark:#0b8f8d;
--ink:#0f172a;/* slate-900 */
--muted:#475569;/* slate-600 */
--bg:#ffffff;
--bg-soft:#f8fafc;/* slate-50 */
}
*{box-sizing:border-box}
html,body{margin:0;padding:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,"Apple Color Emoji","Segoe UI Emoji";color:var(--ink);background:var(--bg);}
a{color:var(--brand);text-decoration:none}
a:hover{text-decoration:underline}
.container{max-width:1100px;margin:0 auto;padding:0 20px}
header{position:sticky;top:0;background:rgba(255,255,255,.85);backdrop-filter:saturate(1.5) blur(6px);border-bottom:1px solid #e2e8f0;z-index:50}
.nav{display:flex;align-items:center;justify-content:space-between;height:64px}
.logo{display:flex;gap:10px;align-items:center;font-weight:700;letter-spacing:.2px}
.logo .mark{width:28px;height:28px;border-radius:8px;background:linear-gradient(135deg,var(--brand),#8be9e5)}
nav ul{display:flex;gap:20px;list-style:none;margin:0;padding:0}
nav a{color:var(--ink);font-weight:500}
.btn{display:inline-block;padding:12px 16px;border-radius:12px;border:1px solid transparent;background:var(--brand);color:#fff;font-weight:600;box-shadow:0 6px 20px rgba(14,165,163,.2);transition:.2s}
.btn:hover{background:var(--brand-dark);transform:translateY(-1px)}
.btn.ghost{background:transparent;border-color:#cbd5e1;color:var(--ink)}
.hero{display:grid;grid-template-columns:1.1fr 0.9fr;gap:32px;align-items:center;padding:72px 0}
.hero h1{font-size:48px;line-height:1.06;margin:0 0 12px}
.hero p{font-size:18px;color:var(--muted);margin:0 0 24px}
.hero .korean{font-size:14px;color:#64748b;margin-top:6px}
.hero .cta{display:flex;gap:12px;flex-wrap:wrap}
.hero .card{background:var(--bg-soft);border:1px solid #e2e8f0;border-radius:20px;padding:20px}
.hero img{width:100%;border-radius:20px;box-shadow:0 10px 35px rgba(2,6,23,.15);object-fit:cover}

.section{padding:64px 0}
.section h2{font-size:32px;margin:0 0 12px}
.section p.lead{color:var(--muted);margin:0 0 24px}

.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.card{border:1px solid #e2e8f0;border-radius:16px;padding:20px;background:#fff}
.card h3{margin:0 0 8px;font-size:20px}
</html>
